---
title: "Windows 身份验证的概念"
description: "Windows Server 安全"
ms.custom: na
ms.prod: windows-server-threshold
ms.reviewer: na
ms.suite: na
ms.technology: security-windows-auth
ms.tgt_pltfrm: na
ms.topic: article
ms.assetid: 29d1db15-cae0-4e3d-9d8e-241ac206bb8b
author: coreyp-at-msft
ms.author: coreyp
manager: dongill
ms.date: 10/12/2016
ms.openlocfilehash: 27e124c971926edd33f102fe6009a0c552c6d814
ms.sourcegitcommit: db290fa07e9d50686667bfba3969e20377548504
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 12/12/2017
---
# <a name="windows-authentication-concepts"></a>Windows 身份验证的概念

>适用于：Windows Server（半年通道），Windows Server 2016

此参考概述主题介绍了基于 Windows 身份验证的概念。

验证是用于对象或人的身份进行验证过程。 当你验证身份一个对象时，目标是验证对象是正版。 当你的用户身份验证时，旨在验证的人不入侵来说。

在网络的上下文，身份验证是到的网络应用程序或资源证明身份的操作。 通常，身份证明通过加密操作任一键仅用户知道 （的公用加密），则使用或共享的键。 身份验证更换费用的服务器端比较用已知密钥来验证身份验证尝试签名的数据。

存储在中心位置安全密钥使身份验证过程，可缩放和维护。 推荐的 Active Directory 并且默认技术来存储身份的信息，包括加密按键，为用户的凭据。 Active Directory 是必需的默认 NTLM 和 Kerberos 实现。

身份验证方法范围是从简单登录到操作系统或登录到服务或应用，标识用户基于某些内容，仅用户知道，密码，如到某些内容公钥证书的标记为用户 has'such 图片、 使用的功能更强大安全机制或生物属性。 在企业环境中，用户可能会访问上多种类型的服务器，在一个位置或多个位置跨多个应用。 鉴于上述原因，身份验证必须支持环境对其他平台和其他 Windows 操作系统。

## <a name="authentication-and-authorization-a-travel-analogy"></a>身份验证和授权： 旅行类似
旅行对比有助于身份验证的工作方式的说明。 几个准备任务是通常需要以此着手。 旅行必须向其主机主管机构证明自己真正的身份。 此证明可以在以下形式出现： 证明公民、 出生日期位置、 个人凭证、 照片或任何所需的主机国家/地区的法律。 旅行的身份验证的护照，类似于系统帐户发放和管理由组织-主体的安全颁发。 法律法规政府颁发机构颁发一系列基于 passport 和预期的目标基于。

**协作**

当旅行到达国际边框时，边框 guard 要求输入凭据，，并旅行呈现他或护照。 该进程是双重：

-   通过验证，它是安全颁发机构颁发本地政府信任 （信任，至少，处理护照），并验证 passport 未进行修改，guard 对 passport 进行身份验证。

-   Guard 验证旅行，通过验证面部匹配 passport 上图中显示的人脸和其他所需的凭据处于良好状态。

如果 passport 证明有效，旅行证明以将其所有者身份验证是否成功，并旅行可以跨边框允许访问。

传递信任之间安全颁发机构是身份验证; 基础发生在国际的边框的身份验证类型取决于信任。 本地政府不知道旅行，但它信任主机政府无法。 当主机政府发出 passport 时，它并不知道旅行任一。 它受信任的出生日期证书或其他文档颁发机构。 反过来，颁发出生证书，该机关受信任的医师签名证书。 医生见证旅行的出生日期和标记证书直接证明身份，这种情况下与新生儿的占用情况。 这种方式，通过受信任中介，传输的信任是传递。

传递信任是基础 Windows server 客户端/体系结构中的网络安全。 信任关系排在域中，如域树中，一套整个，并形成之间域和所有信任该域的域的关系。 例如，如果域具有传递信任与域 B，并且域 B 信任域 C，然后域 A 信任域 c。

没有身份验证和授权之间的区别。 身份验证，系统证明你是您所说的人。 在授权，系统将验证你有权利你想要执行什么操作。 若要拍摄下一步的边框相似之处，只需进行身份验证旅行正确的有效 passport 所有者不一定授权旅行输入一个国家或地区。 特定国家/地区的居民能够通过只需仅在某些情况下，在输入的国家/地区无限可用于授予权限所有公民该特定国家/地区输入呈现 passport 输入其他国家/地区。

同样，你可以授予所有用户的某些域权限来访问资源。 任何用户都将属于该域有权访问资源，，就像加拿大允许美国公民输入加拿大。 但是，在尝试输入巴西或印度美国公民找到，只需的呈现 passport，因为这些国家/地区都需要访问美国公民具有有效 visa，他们不能输入这些国家/地区。 因此，验证不能保证资源或使用资源授权的访问。

## <a name="credentials"></a>凭据
Passport 和关联可能会 visas 是旅行接受的凭据。 但是，这些凭据可能不允许输入或访问国家/地区内的所有资源旅行。 例如，必须其他凭据参加会议。 在 Windows 中，可以管理凭据，以使帐户持有人访问通过网络资源，无重复提供自己的凭据。 访问此类让用户由系统访问所有应用进行身份验证一次和数据源他们授权使用无需输入其他标识符帐户或密码。 Windows 平台大写本地缓存的凭据用户在操作系统的本地安全颁发机构 (LSA) 通过网络使用的单个用户身份 （由 Active Directory 维护） 的能力。 当用户登录到域时，Windows 身份验证程序包坦诚使用凭据提供单一登录时验证网络资源的凭据。 有关的凭据的详细信息，请参阅[在 Windows 身份验证的凭据进程](credentials-processes-in-windows-authentication.md)。

多重身份验证的旅行的形式可能需要执行并呈现多份文档验证其身份 passport 和会议的登记信息等。 Windows 实现了此表单或身份验证通过智能卡、 虚拟智能卡和生物技术。 

## <a name="security-principals-and-accounts"></a>安全主体和帐户
在 Windows 中，任何用户、 服务、 组或可以开始操作的计算机处于安全主体。 安全主体有帐户，可以将计算机的本地也是基于域。 例如，Windows 已加入域的客户端可以加入了网络域通过与域控制器进行通信，没有人工用户登录时，即使。 若要启动通信，的计算机必须具有域中的活动的帐户。 之前接受从计算机的通信，域控制器在本地安全颁发机构验证的计算机的身份，并再人工安全主体一样定义计算机安全环境。 此安全上下文定义特定计算机或用户、 服务、 组中或在网络上的计算机上身份和功能的用户或服务。 例如，它定义的资源文件共享或打印机，可访问和的操作，如阅读、 写入或修改，可以执行的用户、 服务或计算机资源上的等。 有关详细信息，请参阅[安全主体](https://technet.microsoft.com/itpro/windows/keep-secure/security-principals)。

帐户是一种方法以找出 claimant-人工用户或服务-请求访问或资源。 旅行持有可信 passport 所拥有的主机国家/地区有的帐户。 用户，组的用户、 对象和服务都可以有个人帐户或共享帐户。 帐户可的组成员，并且可以在特定的权利和权限分配。 客户可以将本地计算机、 工作组、 网络限制或即可分配到某个域的会员。

内置的帐户和的成员的安全组定义每个版本的 Windows。 通过使用安全组，你可以指定相同的安全权限到成功进行身份验证，许多用户从而简化了的访问权限管理。 规则发出护照可能需要旅行，即可分配到某些组，例如企业，或游客或政府。 此过程确保在一个组中的所有成员一致安全权限。 使用安全组分配权限常且易于管理和审核将保持意味着访问资源的控制。 通过添加和删除根据需要则需要从适当的安全组的访问权限的用户，你可以最大程度减少更改访问 acl) 的频率。

独立托管服务帐户和 Windows Server 2008 R2 和 Windows 7 提供必要的应用程序，如 Microsoft Exchange Server 和 Internet 信息服务 (IIS)，使用他们自己的域帐户，隔离时手动不再需要的管理员管理服务主要名称 (SPN) 并为这些帐户凭据中引入了虚拟帐户。 组托管的服务帐户引入了在 Windows Server 2012 和提供相同的功能在域中，但也可以在多台服务器扩展该功能。 连接到服务器电场的日落，如网络加载余额，在托管服务时需要支持相互身份验证身份验证协议，所有实例服务的都使用的相同的原则。

有关帐户的详细信息，请参阅：

-   [Active Directory帐户](https://technet.microsoft.com/itpro/windows/keep-secure/active-directory-accounts)

-   [Active Directory安全组](https://technet.microsoft.com/itpro/windows/keep-secure/active-directory-security-groups)

-   [本地帐户](https://technet.microsoft.com/itpro/windows/keep-secure/local-accounts)

-   [Microsoft帐户](https://technet.microsoft.com/itpro/windows/keep-secure/microsoft-accounts)

-   [服务帐户](https://technet.microsoft.com/itpro/windows/keep-secure/service-accounts)

-   [特殊的身份](https://technet.microsoft.com/itpro/windows/keep-secure/special-identities)

## <a name="delegated-authentication"></a>委派身份验证
若要使用的旅行相似之处，国家/地区可能发出相同的访问权限的所有成员的官方政府委派，只要委托是已知。 这种委派使一个加以处理的其他成员颁发的成员。 在 Windows 中，委派身份验证或时出现网络服务接受收到用户身份验证请求以启动到第二个网络服务的新连接假定该用户的身份。 支持委派身份验证，你必须建立前端或第一层服务器，如 web 服务器，负责处理射身份验证的后端或 n 层服务器，如大的数据库，负责存储的信息。 你可以委派设置减少你的管理员管理负载委派给你的组织中的用户的身份验证的权利。

通过为信任委派建立的服务或计算机，你可以让该服务或计算机完成委派身份验证接收发出请求时，用户票证，然后访问该用户的信息。 此模型将限制后台服务器上的数据访问只需这些用户或服务该存在凭据以访问正确控制标记。 此外，它允许访问审核这些后端资源。 通过要求的所有数据进行都访问通过委派给代表客户端的使用的服务器的凭据，您将确保服务器无法威胁，并，你可以访问敏感信息的一部分存储在其他服务器上。 身份验证的代理可用于多层旨在在多台计算机上使用单个登录功能的应用程序。

### <a name="authentication-in-trust-relationships-between-domains"></a>在域之间信任关系身份验证
访问位于在不同的域中的共享的资源，就像旅行允许于不同地区的国家/地区旅游的用户需要具有多个已合法的域的大多数企业。 控制此访问需要，还会经过身份验证和授权可以使用另一个域中的资源一个域中的用户。 为了提供身份验证和授权的客户端和服务器不同域中的之间的功能，必须有信任之间的两个域。 信任都是安全的 Active Directory 通信发生和是 Windows Server 网络体系结构不可或缺的安全组件用户的基础技术。

在两个域之间存在信任，为每个域身份验证机制信任来自其他域身份验证。 信任帮助通过验证该传入的身份验证来提供对在资源域-信任域-共享资源的控制访问请求来自受信任的授权-受信任的域。 这种方式，只允许的桥验证身份验证请求旅行域之间充当信任。

如何特定信任传递身份验证请求取决于配置的方式。 信任关系可，通过提供来自受信任的域中访问信任的域中，在资源双向，通过提供的每个域访问其他域中的资源。 信任还有任一不可传递，仅之间两个信任合作伙伴域，或者传递存在区分信任、 信任这种情况下自动延伸到任何其他合作伙伴任一信任的域。

有关信任的工作原理的信息，请参阅[如何域和森林信任工作](https://technet.microsoft.com/library/cc773178(v=ws.10).aspx)。

### <a name="protocol-transition"></a>协议转换
协议转换协助允许通过切换到安全功能，如相互身份验证的 Kerberos 协议和约束的委派，后续应用程序层方面，从而支持在用户身份验证层和不同的身份验证架的应用程序的应用程序的设计。

有关协议转换的详细信息，请参阅[Kerberos 协议过渡和受限委派](https://technet.microsoft.com/library/cc758097(v=ws.10).aspx)。

### <a name="constrained-delegation"></a>约束的委派
约束的委派使管理员指定和履行应用程序信任边界通过限制应用程序服务可以代表用户范围的功能。 你可以指定委派的计算机可以从该请求资源特定服务。 灵活地约束服务授权权限的应用程序的安全设计通过减少帮助提高破解的几率由不受信任的服务。

有关受限制的委派详细信息，请参阅[Kerberos 受限委派概述](../kerberos/kerberos-constrained-delegation-overview.md)。

## <a name="see-also"></a>请参阅
[Windows 登录并验证 Technical 概述](https://technet.microsoft.com/library/dn269029.aspx)


